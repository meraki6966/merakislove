# Soulful Tech — merakislove.com
## Claude Code Project Brief

> Hand this file to Claude Code as your first prompt context.
> It contains everything needed to scaffold the full site correctly on the first pass.

---

## Project Overview

Portfolio/agency site for Meraki is Love, LLC (brand: Soulful Tech).
Full-stack product studio. AI integration. Security posture. Built with intention.

- **URL:** merakislove.com
- **Owner:** Adam McClarin, Friendswood TX
- **Deploy target:** Vercel
- **Framework:** Next.js 16.2 (App Router)
- **Styling:** Tailwind CSS + CSS Modules for bespoke animation components
- **Animation:** Framer Motion
- **Fonts:** Cormorant Garamond (display), Space Grotesk (body/UI), JetBrains Mono (code/meta)
- **CMS:** None (Phase 1). Static MDX for case studies if needed later.

---

## File Structure

```
merakislove/
├── app/
│   ├── layout.tsx              # Root layout: fonts, metadata, grain overlay, cursor
│   ├── page.tsx                # Home page
│   ├── work/
│   │   └── page.tsx            # Work / portfolio page
│   ├── services/
│   │   └── page.tsx            # Services page
│   ├── about/
│   │   └── page.tsx            # About page
│   └── contact/
│       └── page.tsx            # Contact page
├── components/
│   ├── Nav.tsx                 # Fixed navigation with scroll-aware background
│   ├── Footer.tsx              # Footer CTA + copyright + social links
│   ├── Cursor.tsx              # Custom amber cursor with hover ring (client component)
│   ├── GrainOverlay.tsx        # SVG noise texture at 4% opacity
│   ├── ScrollReveal.tsx        # IntersectionObserver wrapper for scroll animations
│   ├── TiltCard.tsx            # Perspective tilt card (the showpiece interaction)
│   ├── SectionDivider.tsx      # 40px amber line divider
│   ├── Tag.tsx                 # Stack tag chip component
│   ├── PageHeader.tsx          # Reusable interior page header with amber left border
│   └── CredibilityStrip.tsx    # Horizontal credential ticker
├── lib/
│   ├── projects.ts             # Project data (VeloxSync, Lingua, Canopy Guard, Meridian)
│   └── services.ts             # Services data
├── styles/
│   ├── globals.css             # CSS variables, scrollbar, base resets
│   └── animations.module.css   # Amber pulse keyframes, grain shift animation
├── public/
│   ├── covers/                 # Case study cover images (from Figma export)
│   │   ├── veloxsync-cover.png
│   │   ├── lingua-cover.png
│   │   ├── canopyguard-cover.png
│   │   └── meridian-cover.png
│   └── noise.svg               # Grain texture SVG
├── next.config.ts
├── tailwind.config.ts
├── tsconfig.json
├── package.json
└── AGENTS.md                   # Auto-generated by create-next-app in 16.2
```

---

## Design Tokens (CSS Variables)

```css
:root {
  --navy: #0B1426;
  --void: #07090F;
  --amber: #F59E0B;
  --amber-dim: #F59E0B18;
  --amber-glow: #F59E0B30;
  --purple: #2D005E;
  --gold: #C8A96E;
  --smoke: #E8E4DC;
  --smoke-dim: #B0A898;
  --muted: #556677;
  --deep-muted: #2A3A4A;
  --border: #131D2E;
  --border-mid: #1A2840;
}
```

---

## Font Setup (next/font)

```tsx
import { Cormorant_Garamond, Space_Grotesk, JetBrains_Mono } from 'next/font/google';

const cormorant = Cormorant_Garamond({
  subsets: ['latin'],
  weight: ['300', '400', '600', '700'],
  variable: '--font-display',
  display: 'swap',
});

const spaceGrotesk = Space_Grotesk({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600'],
  variable: '--font-body',
  display: 'swap',
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ['latin'],
  weight: ['400', '500'],
  variable: '--font-mono',
  display: 'swap',
});
```

---

## Tailwind Config Extensions

```ts
// tailwind.config.ts
const config = {
  theme: {
    extend: {
      colors: {
        navy: '#0B1426',
        void: '#07090F',
        amber: '#F59E0B',
        'amber-dim': 'rgba(245, 158, 11, 0.09)',
        'amber-glow': 'rgba(245, 158, 11, 0.19)',
        purple: '#2D005E',
        gold: '#C8A96E',
        smoke: '#E8E4DC',
        'smoke-dim': '#B0A898',
        muted: '#556677',
        'deep-muted': '#2A3A4A',
        border: '#131D2E',
        'border-mid': '#1A2840',
      },
      fontFamily: {
        display: ['var(--font-display)', 'serif'],
        body: ['var(--font-body)', 'sans-serif'],
        mono: ['var(--font-mono)', 'monospace'],
      },
    },
  },
};
```

---

## Motion Rules

1. **Scroll Reveal:** opacity 0 to 1, translateY 28px to 0, duration 0.75s ease-out. Trigger at 12% intersection.
2. **Cursor Depth (TiltCard):** perspective(900px), max 10deg rotateY/rotateX, translateZ 8px on hover. Ease back on mouse leave with 0.55s cubic-bezier.
3. **Amber Pulse:** box-shadow 0 0 0 to 0 0 32px amber-glow, 3s ease-in-out infinite. Used on the word "Soul" in the hero.
4. **Grain Overlay:** SVG fractal noise at 4% opacity, fixed position, pointer-events none. Subtle animation shift on 8s steps.
5. **Hard limit:** No animation runs longer than 0.8s (except pulse and grain). No bounce. No spring. Motion behaves like breath.

---

## Pages

### Home (/)
1. Hero: full-viewport, dark canvas, Cormorant headline "Where Soul Meets Software", amber pulse on "Soul", two CTAs, vertical accent line, scroll indicator
2. Credibility Strip: CISSP, Azure AI Engineer, Dual MS Cybersecurity UMGC, MBA Baruch, Published Author, 20 Years
3. Featured Work: 3 TiltCards (VeloxSync, Lingua, Canopy Guard) with cover images, stack tags, hover amber border
4. Services Teaser: 4 columns (Product Engineering, AI Integration, Security Posture, Content & Strategy) with real proof lines
5. Philosophy Block: purple gradient section, Cormorant italic quote, explanation paragraph, no CTA
6. Footer CTA: "Have a project in mind?", Calendly button, social links, copyright

### Work (/work)
1. Page header with amber left border
2. 4 TiltCards: VeloxSync for Education, Meraki Lingua, Canopy Guard, Meridian AI
3. Confidentiality note with Calendly CTA

### Services (/services)
1. Page header
2. 4 service pillars with numbered sections and proof lines
3. Engagement models (Hourly $85/hr, Project fixed scope, Retainer coming soon)
4. Process: Discovery, Architecture, Build, Deploy and Harden
5. CTA with purple gradient border

### About (/about)
1. Page header: "Adam McClarin" with Reiki practitioner subtitle
2. The Story (3 paragraphs, full copy provided below)
3. Credentials grid: CISSP, Azure AI Engineer, Dual MS Cybersecurity UMGC 2018, MBA Baruch 2014, 20 years
4. Published Work: The Weight of the Mantle, The AI Prompt Playbook, Radical Endurance (Aris Vala), Whispers of the Veil

### Contact (/contact)
1. Page header
2. Two TiltCard paths: "Start a Project" (new prospects Calendly) and "Client Discussion" (existing clients Calendly)
3. Direct contact: location, social links, studio info

---

## About Page Story Copy (Final)

**Paragraph 1:**
The day starts in stillness. Before the laptop opens, before the first message lands, before the build resumes where it stopped the night before, there is a few minutes of nothing. Breath, quiet, and intention. That practice is not separate from the engineering. It is the reason the engineering looks the way it does.

**Paragraph 2:**
Twenty years across leadership, IT, and information security, and for most of them I believed I was helping people. I was building the systems, closing the tickets, passing the audits, doing the work the way it was supposed to be done. Then it hit me. The systems were serving the org chart, the compliance checklist, the quarterly number. The person on the other side of the screen was an afterthought, if they were thought of at all. That is the moment Meraki is Love started, not as a business plan, but as a refusal to keep building that way.

**Paragraph 3:**
So now the work answers to a different standard. VeloxSync gives K-12 teachers AI that moves the way teachers actually think, by grade band, against real state standards. Meraki Lingua opens conversation across 37 language communities, dialect by dialect, instead of flattening everyone into one voice. Canopy Guard hands a small business the kind of security posture report that enterprise clients pay consultants six figures to produce. The meditation and the Reiki practice are not a hobby that lives next to the technical work. They are the source of the pace, the attention, and the decision to stop and ask whether a feature actually serves the person using it. Soulful Tech is not a tagline I picked because it sounded warm. It is the only way I know how to build.

---

## Project Data

```ts
// lib/projects.ts
export const projects = [
  {
    name: "VeloxSync for Education",
    slug: "veloxsync-education",
    type: "SaaS Platform",
    year: "2025",
    description: "K-12 AI platform with grade-band intelligence. 10 DB tables, 30+ endpoints, 112 state standards, Ei-Core fine-tuned models.",
    stack: ["Next.js", "Supabase", "Together.ai", "Railway"],
    stat: "K-12 · 112 state standards",
    glow: "#3B82F6",
    coverBg: "#0A1E3A",
    cover: "/covers/veloxsync-cover.png",
  },
  {
    name: "Meraki Lingua",
    slug: "meraki-lingua",
    type: "AI Widget",
    year: "2025",
    description: "37-language AI chat widget. Dialect-accurate, RTL-supported, Claude API-powered. HTML PDF export built in.",
    stack: ["Claude API", "React", "Node.js", "Vercel"],
    stat: "37 languages · RTL support",
    glow: "#10B981",
    coverBg: "#0F1A0A",
    cover: "/covers/lingua-cover.png",
  },
  {
    name: "Canopy Guard",
    slug: "canopy-guard",
    type: "Security Tool",
    year: "2025",
    description: "Client security posture auditor. MITRE ATT&CK mapped findings. CISSP-informed. Branded client-ready reports.",
    stack: ["Next.js", "TypeScript", "Vercel", "MITRE"],
    stat: "CISSP-informed · ATT&CK mapped",
    glow: "#EF4444",
    coverBg: "#1A0A0A",
    cover: "/covers/canopyguard-cover.png",
  },
  {
    name: "Meridian AI",
    slug: "meridian-ai",
    type: "Executive Platform",
    year: "2024",
    description: "Executive intelligence platform with MFA, RBAC, and multi-format export. PPTX, PDF, and HTML output from a single workflow.",
    stack: ["React", "Node.js", "SendGrid", "Cloudinary"],
    stat: "PPTX · PDF · HTML export",
    glow: "#8B5CF6",
    coverBg: "#0D0A1E",
    cover: "/covers/meridian-cover.png",
  },
];
```

---

## External Links

- New prospects Calendly: https://calendly.com/hello-merakislove/new-meeting
- Existing clients Calendly: https://calendly.com/hello-merakislove/client-discussion
- Figma covers file: https://www.figma.com/design/gkcIsjz4Ewk8SNBwVtjSM7

---

## Claude Code Opening Prompts

Use these in sequence after scaffolding:

**Prompt 1 — Scaffold:**
```
Create a new Next.js 16.2 project using create-next-app with TypeScript, Tailwind CSS, App Router, and src directory disabled. Name it merakislove. Install framer-motion as a dependency. Set up the three Google Fonts (Cormorant Garamond, Space Grotesk, JetBrains Mono) using next/font in the root layout. Add the CSS variables and Tailwind config extensions from the project brief. Create the file structure outlined in the brief.
```

**Prompt 2 — Shared Components:**
```
Build the shared components: Nav.tsx (fixed nav with scroll-aware background blur, mobile hamburger, Book a Call button), Footer.tsx (CTA section + copyright), Cursor.tsx (custom amber dot cursor with hover ring, client component), GrainOverlay.tsx (SVG noise at 4% opacity), ScrollReveal.tsx (Framer Motion wrapper, triggers at 12% intersection, translateY 28px to 0), TiltCard.tsx (perspective tilt on mouse move, max 10deg, amber border on hover), PageHeader.tsx (amber left border accent), Tag.tsx, SectionDivider.tsx, CredibilityStrip.tsx. Use the design tokens from the project brief.
```

**Prompt 3 — Home Page:**
```
Build the home page with all six sections from the project brief: Hero with amber pulse on "Soul", Credibility Strip, Featured Work with 3 TiltCards, Services Teaser with 4 columns, Philosophy Block with purple gradient, and Footer CTA. Use placeholder images for the covers. Import project data from lib/projects.ts.
```

**Prompt 4 — Interior Pages:**
```
Build the four interior pages: Work (4 TiltCards + confidentiality note), Services (4 pillars + engagement models + process steps + CTA), About (story copy from the brief + credentials grid + books), Contact (two TiltCard Calendly paths + direct contact info). Use the PageHeader component on all interior pages.
```

**Prompt 5 — Polish:**
```
Run a full polish pass: verify all scroll reveals fire correctly, test the TiltCard interaction on all cards, check mobile responsive breakpoints on every page, verify the grain overlay renders without performance issues, ensure the Nav blur activates on scroll, and confirm all Calendly links open in new tabs. Add proper metadata to each page for SEO.
```

---

## Known Issues to Resolve During Build

1. **merakislove.com 200-on-all-paths:** The current hosting setup returns 200 for every path. Migrating to this new Vercel deploy resolves it. Verify after deploy that non-existent routes return proper 404s.
2. **Cover images:** Placeholder treatment until Figma exports are ready. Use a dark gradient card with the project name as text and the project glow color as a subtle radial gradient.
3. **Social links:** LinkedIn, GitHub, Substack URLs need to be wired to Adam's actual profiles.

---

*Generated from the Soulful Tech Visual Identity Brief, Site Architecture, and Phase 3 build artifact.*
*May 29, 2026*
